# Architecture

<!--
  Describe your agent's architecture here. This document is part of your submission
  and will be used in evaluation. Cover the following:

  1. **Design overview** — How does your agent work? What's the high-level flow?
  2. **Dependency resolution** — How does your agent handle endpoints that need data
     from other endpoints (e.g., fetching an ID before calling a detail endpoint)?
  3. **Avoiding false negatives** — How does your agent minimize misclassifying valid
     endpoints as invalid due to its own mistakes (bad params, missing body, etc.)?
  4. **Classification logic** — How does your agent decide between valid, invalid_endpoint,
     insufficient_scopes, and error?
  5. **Tradeoffs** — What tradeoffs did you make? What would you improve with more time?
  6. **Architecture pattern** — Why did you choose your particular pattern (single agent,
     multi-agent, orchestrator, etc.)? What are the pros and cons?
-->
